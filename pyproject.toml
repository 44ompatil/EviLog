[project]
name = "evilog"
version = "0.1.0"
description = "EviLog prototype project"
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
